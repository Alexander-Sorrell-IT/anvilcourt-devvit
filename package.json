{
  "name": "receipts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End silent moderation — auto removal explanations + searchable audit log for Reddit (Devvit).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
